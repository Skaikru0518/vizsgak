import { db } from '../db';
import { Request, Response, NextFunction } from 'express';

export async function postMakeFavourite(
  req: Request,
  res: Response,
  next: NextFunction,
): Promise<any> {}
